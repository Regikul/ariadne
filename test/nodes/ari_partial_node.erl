%% @doc Узел с неполным контрактом: объявляет слоты, но не обрабатывает события.
-module(ari_partial_node).

-export([input/0, output/0]).

%% @doc Объявляет единственный входной слот.
input() ->
    [input].

%% @doc Объявляет единственный выходной слот.
output() ->
    [output].
