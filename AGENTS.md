## База знаний

Знания проекта проиндексированы по адресу [okf/index.md](okf/index.md).

## Erlang

Агенты используют `./silent_rebar3` вместо `rebar3`. `./silent_rebar3 test`
прогоняет eunit и печатает покрытие по модулям; тестовые узлы из
`test/nodes` в покрытие не входят.
